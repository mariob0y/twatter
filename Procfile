# this command seem to execute well
# but it fact playwright calls don't work
# only way to make bot work  is to connect remotely
# and run "playwright install" manually

# worker: sh -c 'playwright install'
web: gunicorn wsgi:app
